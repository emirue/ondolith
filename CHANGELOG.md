# CHANGELOG

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/), 버전은
[Semantic Versioning](https://semver.org/lang/ko/)을 따른다.

**업그레이드 시 읽는 법:** `⚠️ 파괴적 변경` 표시가 없으면
[D70](docs/70-operations.md)의 3단계(백업 → 바이너리 교체 → 재시작)가 전부다.
표시가 있으면 그 항목만 추가로 읽는다.

되돌릴 수 없는 마이그레이션이 포함된 릴리즈는 여기에 반드시 적는다 (NFR-308).

---

## [Unreleased]

### Security
- **Go 툴체인을 `1.26.6` 으로 올린다** (NFR-209). `1.26.5` 의 표준 라이브러리에
  우리 호출 경로를 지나는 취약점 **7 건**이 있었다 — `GO-2026-6218`(`net/url`) ·
  `-6091`(`html/template`) · `-6090`(`crypto/tls`) · `-6089`·`-5026`(`net/http`) ·
  `-6088`(`encoding/xml`) · `-5972`(`encoding/asn1`). **`html/template` 은 테마
  렌더링 경로 전체가 올라앉은 패키지다** ([DEC-3.1](.ai/DECISIONS.md)). 올린 뒤 0 건.
  **표준 라이브러리 취약점은 툴체인을 올려야만 고쳐진다** — 의존성 업데이트로는 안 된다.
  `1.26.5` 는 2026-08-03 기준 최신이었고 그때는 0 건이었다: 툴체인은 가만히 있어도
  낡는다. 사람이 아니라 **CI 가 찾았다** — `make vuln` 이 매 푸시마다 돌기 때문에
  뒤처진 날 바로 빨간불이 됐고, 그것이 이 잡을 넣은 이유다
- **모든 응답이 보안 헤더를 진다** (NFR-211). `X-Frame-Options: DENY` ·
  `Content-Security-Policy` (`frame-ancestors`·`object-src`·`base-uri`·`form-action`) ·
  `X-Content-Type-Options: nosniff` · `Referrer-Policy`. 하나도 없었다 —
  **클릭재킹이 열려 있었다.** CSRF 검사(`CrossOriginProtection`)는 요청이 어디서
  왔는지를 보는데, 공격자가 이 사이트를 iframe 에 넣고 사용자가 그 안의 진짜
  버튼을 누르면 그 요청은 프레임 자신에게서 나오므로 same-origin 이라 그대로
  통과한다. `internal/httpsec` 한 곳에 두고 **설치·운영 양 트리**가 같은 것을
  쓴다 — 설치 화면은 DB 비밀번호를 받는 폼이라 대가가 더 크지, 덜하지 않다.
- **관리자 이메일이 공개 화면에 노출되지 않는다** (NFR-212). 설치가
  `display_name` 에 관리자 이메일을 그대로 넣고 있어, 관리자가 쓴 글의 작성자
  줄에 `admin@example.com` 이 찍혔다 — 방문자 누구나 읽었고, 로그인 응답이 계정의
  존재를 감추는 것(FR-201)도 그 줄 하나로 무의미했다. 설치 폼에 표시 이름을
  추가하고, 비워 두면 「관리자」를 쓴다. **브라우저 스크린샷에서 보고 알았다.**

### Added
- **CI** (`.github/workflows/ci.yml`) — `make check` 에 더해 **PostgreSQL 18 을 붙여
  `make test-integration` 을 돌린다.** DSN 없이 돌리면 973 건 중 450 건이
  건너뛰고(46%) 그 상태에서도 게이트가 초록이라, 돈이 걸린 경로(결제·환불·반품)를
  실제로 실행하는 곳이 어디에도 없었다 ([GAP-06](docs/85-gaps.md)). `make vuln` 도
  여기서 돈다 — 네트워크가 필요해 오프라인 게이트에 넣을 수 없다.
  **첫 실행이 곧바로 두 가지를 찾았다**: 툴체인 취약점 7 건(위 Security 항목)과,
  아래 selftest 의 환경 가정. 로컬에서 초록이라고 CI 에서 초록인 것이 아니다
- **릴리즈 파이프라인** (`.github/workflows/release.yml`) — `v*` 태그에 게이트를
  다시 돌리고 linux amd64/arm64 바이너리와 `SHA256SUMS` 를 낸다. 체크섬이 없으면
  받는 쪽은 받은 것이 우리가 낸 것인지 확인할 방법이 없다. 이어서 비공개
  사이트 저장소에 `repository_dispatch` 로 알린다 — **쓰지 않고 알리기만 한다.**
  공개 저장소의 워크플로가 비공개 쪽에 직접 쓰면, 공개 워크플로를 고칠 수 있는
  사람이 곧 비공개 쪽에 쓸 수 있는 사람이 된다
- **Phase 표시 검사** (checkdocs 31) — [D80](docs/80-roadmap.md) 의 Phase 표시를
  [D81](docs/81-work-breakdown.md) 의 완료 상태와 대조한다. 작업 127 건 중 124 건이
  완료된 뒤에도 D80·README·CLAUDE.md 는 전부 「Phase 0 설치 마법사 진행 중」이라고
  적혀 있었고 게이트는 초록이었다 — 진입점이 네 단계 낡으면 그것을 읽고 시작하는
  사람은 이미 있는 것을 다시 만든다. README·CLAUDE.md 에서는 상태 문장을 **지웠다**:
  복사본이 없으면 어긋날 수도 없다.
  **세는 쪽은 마커(`**(완료`)를 본다.** 첫 판은 「완료」를 부분 문자열로 찾았고,
  그러자 W3-34 의 `**(절차 완료 — …)**`(절차만 있고 실측은 없다, [GAP-03](docs/85-gaps.md))
  와 W4-13 의 제목에 든 「완료 기준 실측」이 **완료로 집계됐다.** 그 숫자를 믿고
  Phase 3·4 에 ✅ 를 붙였는데 실제는 124/127 이고 둘 다 진행 중이다 — 대장이
  내내 맞았고 표시가 틀렸다. 같은 부류로 이미 두 번 당했다(BRE 의 `\(`, BSD sort
  의 collation): **검사가 도는 것과 검사가 보는 것은 다르다** ([M4](.ai/MISTAKES.md))
- **D19 오류표 상태코드 검사** (checkdocs 30-1) — 0.3 절이 선언한 코드만 오류표에
  쓸 수 있다. 0.3 은 여섯 종을 선언했는데 표는 `200`·`400`·`413`·`500`·`502` 를
  백 번 가까이 썼다 — 선언에 없는 코드는 무엇을 뜻하는지 합의된 적이 없다는
  뜻이고, 실제로 **재고 네 행(P-401·P-403·P-406·P-512)이 `400` 으로 적힌 채
  구현은 전부 `422`** 였다. 0.3 에 다섯 종을 뜻과 함께 선언하고(`200` 은 계정
  열거 방지·웹훅 멱등처럼 **실패인데 성공과 같은 응답**을 주는 자리다), 네 행을
  `422` 로 고쳤다. `400` 과 `422` 의 경계는 **「읽었는가」** 로 못 박았다 —
  `quantity=abc` 는 읽지 못하므로 400, `quantity=999` 는 읽었는데 재고가 5 라
  받아들일 수 없으므로 422 다
- **`internal/httpsec` 테스트** — 이 패키지는 전부 보안 통제인데 테스트가 0 건이었고,
  `app` 쪽의 간접 단언은 「비어 있지 않다」 수준이라 `Referrer-Policy: unsafe-url` 도
  CSP `form-action` 삭제도 통과시켰다(둘 다 실제로 지워 확인했다). 헤더 값을
  통째로, 지시어를 이름별로 못 박는다
- **`internal/auth` 토큰 생성기 테스트** — `newRawToken`·`hashToken`·`ttl`·
  `sprintfTable` 은 DB 를 하나도 쓰지 않는데 DSN 뒤에 있어 `make check` 에서 한 번도
  실행되지 않았다. `newRawToken` 이 상수를 돌려주게 고쳐도 — 즉 모든 재설정·인증
  토큰이 같은 값이 되어도 — 전 패키지가 초록이었다
- **[D85](docs/85-gaps.md) 결함·미검증 대장** — 게이트가 전부 초록이어도 확인되지
  않은 것(`GAP-##`)과 알려진 결함(`BUG-##`)을 한 곳에 적는다. [D18](docs/18-open-decisions.md)이
  「아직 **정하지** 않은 것」 전용이라 「정해졌는데 안 했거나, 했는데 확인되지 않은
  것」은 갈 곳이 없었다. 미결 대장과 같은 검사를 건다: 대장에 없는 번호를 인용하면
  `make check` 실패 — 구멍을 메우고 행을 지웠는데 다른 곳이 열린 것으로 말하는
  일이 `OPEN-##`에서 두 번 있었다. **문서뿐 아니라 `*.go`·`*.sql`까지 훑는다**:
  `BUG-##`가 설명하는 자리는 나중에 고칠 코드이고 그 옆의 주석이 가장 오래 남는
  인용이다 (`OPEN-##`은 실제로 `.go`에 17곳 있다). GAP 행에 **닫는 방법**이 비어도
  실패한다.
- **`make shots`** — 같은 브라우저·같은 시드로 화면을 **찍어** `shots/` 에 남긴다
  (552장 — 라이트·다크 두 벌). `make ui` 는 좌표를 재지만 «정상 크기의 잘못된 것»은 재지 못한다:
  빈 상자는 넘치지 않고, 이메일이 찍힌 작성자 줄도 폭은 정상이며, 밑줄 링크와
  버튼은 같은 높이다. 이 명령으로 찾은 것이 아래 Fixed 의 다섯 항목이다.
- **`make ui`** — 모든 화면을 375·768·1280px 에 그려 놓고 **레이아웃을 잰다**
  (216 조합). `make screens` 는 응답과 본문 길이를, `make crawl` 은 링크를 본다 —
  둘 다 200 을 받으면 통과하므로 글자가 상자 밖으로 나가든 표가 화면을 넘든
  초록이었다. 잡는 것: 가로 넘침, 부모 밖으로 나간 요소, 겹친 테두리, 잘린 글,
  스타일이 붙지 않은 폼 요소, 좁은 화면에서 누를 수 없는 크기, **형제끼리 왼쪽
  어긋남**, **한 화면의 버튼 높이가 세 종류 이상**, **머리·본문·바닥의 세로축
  어긋남**. **라이트와 다크를 각각 잰다** — CSS 에 값이 두 벌인데 한쪽만 재면
  나머지 절반은 아무도 본 적이 없는 화면이다.
  의존성을 늘리지 않는다 — Playwright 가 받아 둔 Chromium 을 CDP 로 직접 몰고
  WebSocket 은 Node 22 내장을 쓴다
- **사이트 네비게이션** — 머리글에 메뉴·검색·장바구니·주문 내역·관리 링크가
  생겼다. 그전까지 **홈에서 눌러서 닿는 화면이 9~11개뿐**이었다: 관리자로
  로그인해도 `/admin/` 으로 가는 링크가 사이트 어디에도 없었고, 쇼핑몰인데
  장바구니와 주문 내역 링크가 없었으며, 구매 이후 화면(영수증·취소·환불·반품·
  교환·구매확정)은 전부 만들어져 있으나 어느 화면도 가리키지 않았다
- **첨부파일** (FR-506) — 글쓰기 폼의 파일 칸과 글 화면의 첨부 목록. FR-506 은
  필수인데 `Attachments.Save` 의 호출자가 **0개**였고, 업로드 디렉터리를 설치도
  부팅도 만들지 않아 화면을 붙여도 저장될 수 없었다. 검증 4겹은 이미 있었다
- **상품 카테고리 생성·삭제** (A-509) — 「이동」만 구현돼 있어 카테고리를 만들
  방법이 없었고, 그래서 P-302 `/shop/c/{slug}` 는 어떤 주소로도 열 수 없었다
- **옵션을 고르면 조합이 바뀐다** (P-303·P-304, FR-602) — 테마에 `hx-` 속성이
  하나도 없어 htmx 를 매 페이지 내려받고 쓰지 않았다. 상품 화면의 옵션 선택은
  아무 동작도 하지 않았고, P-304 는 조각 대신 페이지 전체를 돌려주고 있었다
- **회원 프로필 항목을 운영자가 정의한다** (FR-215, A-406 · `00019_user_fields.sql`).
  지금까지 회원이 가진 것은 이메일·이름·비밀번호뿐이었다. 사이트마다 더 받아야
  할 것이 다르고, 그것을 코드에 적으면 사이트마다 다른 바이너리가 된다.
  **게시판 커스텀 필드와 같은 구조다** ([DEC-3.9](.ai/DECISIONS.md)) — 정의는
  `user_fields` 행, 값은 `users.custom_fields` JSONB. 그래서 **개수 제한이
  없다**: 여분 칸을 고정 개수로 만들어 두는 방식(그누보드가 10개)은 그 수를
  넘는 순간 `ALTER TABLE` 을 요구한다
- **`make crawl`** — 홈에서 시작해 **화면이 만든 링크를 따라간다.** `make e2e`
  와 `make screens` 는 주소를 스크립트가 적으므로 화면이 만드는 주소가 틀려도
  보이지 않는다. 위 결함 대부분이 이 검사로 나왔다
- **회원 탈퇴** (FR-212, P-110) — 비밀번호 확인과 체크박스를 함께 요구한다.
  **계정을 삭제하지 않고 비활성화한다** — 주문의 주체가 사라지면 정산·분쟁
  대응이 불가능해진다. 마지막 관리자 계정은 탈퇴할 수 없다
- **인증 메일 재발송** (FR-214, P-113) — 수신 주소를 입력으로 받지 않는다.
  세션 사용자의 저장된 주소로만 보낸다(스팸 릴레이 방지). 계정당 3회/시간
- **반품·교환** (FR-617, FR-618) — 구매자가 품목·수량을 골라 요청하고, 관리자가
  **수거를 확인해야** 환불이 나간다. 환불액은 `order_items` 스냅샷에서 서버가
  계산하고 반품 배송비는 수거 시점 스냅샷에서 뺀다 — A-512 정책을 나중에 바꿔도
  이미 처리된 건의 금액이 달라지지 않는다. 교환은 **같은 상품의 다른 조합**으로만
  가능하고, 접수 시 새 조합 재고를 예약하며 거부하면 반드시 푼다
- **품목 단위 부분 취소·환불** (FR-625) — 여러 개 산 것 중 하나만 취소할 수 있다.
  화면은 품목과 수량만 받고 **금액은 서버가 주문 시점 스냅샷에서 계산**한다
- **주문 할인** (FR-626) — 할인은 주문 생성 시 품목에 배분되어 스냅샷으로 저장된다.
  부분 취소는 그 스냅샷으로 계산하므로, 전 품목을 나눠 환불한 합계가 결제액과
  **1원도 다르지 않다**
- **비밀번호 재설정** (P-104, P-105) — 메일로 받은 링크로 새 비밀번호를 설정한다.
  링크는 30분·1회용이고 DB에는 해시만 남는다. 요청 화면은 계정이 있든 없든 정지됐든
  같은 답을 돌려준다 (FR-207). 새로 요청하면 앞선 링크는 죽는다
- **Phase 2 게시판** — 게시판·글·댓글·첨부·검색. 게시판마다 테이블을 만들지 않는다
  ([DEC-3.9](.ai/DECISIONS.md)): 게시판은 행이고, 커스텀 필드 스키마는 행이며, 글의 값은
  JSONB 한 컬럼이다. **관리자가 필드를 추가하면 코드 수정 없이 폼과 목록에 나타난다** (FR-503)
- **게시판 단위 권한** — `post.read`·`post.write`·`post.moderate`·`post.read_secret`·
  `comment.write`·`comment.moderate` 6개가 게시판마다 따로 판정된다. 게시판 A 의 조정자가
  게시판 B 를 건드리지 못한다. 게시판 생성 시 권한 프리셋(공개/회원전용/비공개)이 **같은
  트랜잭션에서** 부여 행을 넣는다
- **첨부 업로드** — 확장자 허용목록 · 매직바이트 · 파일명 재생성 · 실행권한 제거 (NFR-206).
  다운로드는 부모 글의 읽기 권한을 **다시** 검사하고 항상 `attachment` 로 내려간다
- **테마 zip 업로드 (FR-307)** — Zip Slip·심볼릭 링크·zip 폭탄·압축비·엔트리 수·깊이 방어.
  실패하면 아무것도 남지 않는다 (임시 디렉터리에 풀고 성공 시에만 rename)
- **전문검색 (FR-507)** — 읽을 수 없는 게시판의 글은 결과에 한 행도 들어가지 않는다
- **작업 로그 (FR-707)** — D15 7절의 기록 대상. 자격증명은 기록할 **필드가 없고**, 표는
  수정·삭제할 수 없다 (DB 트리거)
- `sitemap.xml`·`robots.txt` (FR-510) · 화면별 메타·OG 태그 (FR-511)
- **Phase 1 코어** — 로그인·회원가입·이메일 인증·내 계정, 공개 페이지, 관리자 화면
  (사이트 설정·메일·테마·메뉴·페이지·사용자·역할·시스템 정보). 전 화면이 하나의
  라우트 테이블로 등록되고, 부팅 시 D15 4.4의 자체 점검 5종이 돈다 — 권한 키 오타나
  D11에 없는 화면이 있으면 **기동을 중단한다** (FR-110)
- **RBAC** — 역할 5·Phase 1 권한 19. 권한 사이에 함의를 두지 않고, 상승 방어 R1~R6을
  핸들러에서 강제한다. 마지막 관리자의 비활성·삭제는 잠금으로 직렬화한다 (FR-704)
- **테마 런타임 교체 (FR-302, FR-303)** — 관리자에서 테마를 바꾸면 **다음 요청부터**
  적용된다. 디스크에 템플릿 하나만 놓으면 그 화면만 바뀌고 나머지는 내장 테마가 그린다.
  `theme.json`의 `requires`가 현재 버전보다 높으면 활성화를 거부하고, 개발 빌드에서는
  경고만 한다
- 설치 마법사: 부팅 시 설치/운영 모드 분기, DB 연결·마이그레이션·관리자 생성·설정 기록,
  재시작 없는 운영 모드 전환 (FR-101 ~ FR-108, FR-110)
- 부팅 시 대기 마이그레이션 자동 적용 (NFR-302)
- `ondolith -version` (NFR-305)
- 문서 체계: [docs/](docs/README.md) 인덱스와 D00~D90, `.ai/` 작업 지침
- **세부 기획**: 화면 인벤토리 [D11](docs/11-screens.md)(101개 화면), 공개 화면 상세
  [D12](docs/12-screens-public.md), 관리자 화면 상세 [D13](docs/13-screens-admin.md),
  화면 연계·상태머신 [D14](docs/14-screen-flows.md), 권한 모델
  [D15](docs/15-access-control.md)(역할 5·권한 37·화면 유형 SC-1~SC-8)
- **테마 계약 [D17](docs/17-theme-contract.md) 확정 (FR-308)** — 템플릿 43종, 함수맵,
  뷰 모델 규약, `theme.json`, 폴백 규칙. **필수 파일은 `base.html` 하나**이고 나머지는
  없으면 내장 테마가 대신 그린다. HTML을 렌더링하는 공개 화면 전부에 템플릿이 배정됐는지
  `checkdocs.sh`가 검사한다
- 커머스 미결 5건 결정 (반품+교환 포함 / 증빙은 PG 위임 / 품절 처리 / 약관 필수 동의 /
  사업자 정보 별도 화면). 화면 6개·테이블 4종·상태 전이 8개·권한 `order.return` 추가
- **사이트 유형 설정 (FR-710)** — 일반 사이트(`cms`) / 쇼핑몰(`shop`). 설치 시 고르고
  A-201에서 바꾼다. `cms`면 커머스 라우트를 **등록하지 않는다**(숨기는 것이 아니다).
  마이그레이션은 항상 전체 스키마를 만든다 — 조건부 스키마는 설치처마다 다른 DB를 낳는다
- [D50](docs/50-commerce.md)에 커머스 기능 범위 체크리스트 — 포함 12 / 제외 6 (미결 5건은 전부 결정됐다).
  쿠폰·적립금을 제외한 이유(FR-607 금액 검증의 계산 근거를 바꾼다)를 명시
- 데이터 커버리지 [D16](docs/16-data-coverage.md) — 테이블마다 **만드는 화면·보여주는 화면**을
  적고 `checkdocs.sh`가 빈 칸과 누락 테이블을 잡는다. 이 표를 채우다 누락 화면 7건이 드러났다
  ([M8](.ai/MISTAKES.md))
- 누락 화면 7건 추가: 카테고리 관리(A-509), 송장 입력(A-510), 메일 설정(A-205),
  소셜 설정(A-206), 소셜 계정 연결 관리(P-111), 웹훅 수신 이력(A-603), 구매확정(P-510)
- 테이블 7종 추가: `menus`, `password_reset_tokens`, `social_accounts`, `webhook_events`,
  `operation_logs`, `categories`·`product_categories`, `shipments`
- 요구사항 10건 추가: 회원가입(FR-210), 내 정보(FR-211), 회원 탈퇴(FR-212),
  소셜 계정 연결·해제(FR-213), 메뉴 관리(FR-405), 테마 정적 자산(FR-309),
  상품 검색(FR-614), 상품 카테고리(FR-615), 배송 정보(FR-616),
  메일 발송 설정(FR-708), 소셜 프로바이더 설정(FR-709)
- 문서 작성 규칙 [D90](docs/90-conventions.md)과 이를 강제하는 `scripts/checkdocs.sh`.
  `make check`에 포함된다 (NFR-407). 화면 인벤토리의 `접근`·`상태변경`·`유형`을
  닫힌 어휘로 검사하고, **관리자 화면이 권한 없이 열려 있으면 빌드를 깨뜨린다**
- `scripts/selftest.sh` — 문서 검사기와 gofmt 훅에 위반을 심어 실제로 실패하는지
  검증한다. `make check`에 포함된다 (NFR-405)
- 테스트: 설치/운영 트리 원자적 교체와 동시성(`cmd/ondolith`), 설정 파일 로드·저장과
  깨진 설정을 미설치로 오인하지 않는지(`internal/config`, FR-110), 설치 폼 검증과
  DSN 조립(`internal/install`). `make test`는 `-race`로 돈다
- 통합 테스트 `make test-integration` — 실제 PostgreSQL 대상으로 마이그레이션 적용,
  관리자 생성(bcrypt·소문자 이메일), 설정 파일 권한, 중복 설치 거부, 마이그레이션
  재실행 멱등성을 검증한다 (FR-102~106, FR-108, NFR-302)

- **QR 재고 관리 기획 (FR-620 ~ FR-624)** — 라벨 발행(A-513), 스캔 입고(A-514),
  재고 실사(A-515), 출고 피킹 대조(A-516), 스캔 재고 조회(A-517). **QR은 재고 모델이 아니라
  조합을 특정하는 입력 수단**이라 `product_variants.stock` 정수와 delta 갱신 규칙은 그대로다
  ([D30](docs/30-data-model.md) 무변경). 스티커가 담는 값은 `sku`가 아니라 `product_variants.id`다 —
  SKU는 NULL이 허용되고 바뀔 수 있는데 **창고에 붙은 스티커는 회수할 수 없다**.
  전용 스캐너(키보드 웨지)와 휴대폰 카메라를 **같은 입력창 하나로** 받아 한쪽만 테스트된
  경로를 만들지 않는다. 피킹은 주문 상태도 재고도 바꾸지 않는다 — 재고 차감은 주문 시점에
  이미 끝났고, 전이 주체를 셋으로 늘리지 않기 위해서다

- **Phase 1 마이그레이션 (`00002`~`00005`)** — RBAC 4테이블·시드·콘텐츠 3테이블·인증 부속
  3테이블. D30이 설계만 해두고 **한 번도 실행된 적 없던 10개 테이블이 실제 SQL이 됐다.**
  `permissions`는 Phase 1분 **19개만** 시딩한다 — 37개를 전부 심으면 D15 4.4의 미사용 권한
  경고가 매 부팅 18건을 뱉어 검사 자체가 무시된다. `users.is_admin`은 남긴다 (D30 두 릴리즈
  규칙, W2-01에서 삭제)
- 마이그레이션 통합 테스트 — 실제 PostgreSQL 18에서 **Up·Down·재적용**, `roles_one_superuser_idx`,
  `ON DELETE` 규칙, `CHECK` 제약(프로토콜 상대 URL 거부 포함), `social_accounts` 유니크 2종,
  **시드와 D15 2.5 표의 완전 대조**, `is_admin` 이관을 검증한다. 마이그레이션을 7가지로
  고장내 전부 실패하는 것을 확인했다 ([M4](.ai/MISTAKES.md))
- `checkdocs.sh`의 D30↔SQL 대조를 **모든 마이그레이션·모든 테이블**로 확장 (12종 77컬럼).
  이전에는 `users` 하나만 `00001`과 비교했다
- **RBAC 시드 ↔ D15 대조를 기계화** (43건: 역할 5·Phase 1 권한 19·부여 19). 시드는 D15
  2.2/2.5를 SQL로 옮긴 것인데, 처음엔 통합 테스트가 그 표를 Go 리터럴로 **또 한 번 베꼈다** —
  [M9]·[M11]·[M12]가 세 번 기록한 바로 그 패턴이다. 이제 `checkdocs`가 D15↔SQL을 대조하고
  테스트는 **시드 SQL을 파싱**해 DB와 맞춰, 사슬 어디에도 손으로 베낀 표가 없다
  ([M9](.ai/MISTAKES.md), [M11](.ai/MISTAKES.md), [M12](.ai/MISTAKES.md))

- **실행 루프 [D82](docs/82-execution-loop.md) + `scripts/next-task.sh`** — D81의 126개 작업을
  멈추지 않고 돌리는 절차. 계획은 이미 D81에 있었고 없던 것은 **그것을 실행하는 수단**이다.
  스크립트가 선행이 전부 끝난 작업을 골라 산출물·완료 기준과 함께 낸다. **상태 파일을 두지
  않는다** — 완료 표시는 D81 행에만 남기고, 계획과 진행을 다른 파일에 두면 둘 중 하나가
  반드시 낡는다 ([M9](.ai/MISTAKES.md))
- `next-task.sh`의 종료 코드를 넷으로 분리했다 (0 계속 / 2 완료 / 3 교착 / 1 문서 깨짐).
  처음엔 셋 다 `0`이라 자동 루프가 **"끝났다"와 "막혔다"를 구분할 수 없었다** — 조용히
  성공한 것처럼 보이는 [M3](.ai/MISTAKES.md) 계열이다. `selftest.sh`가 픽스처 6종으로 고정한다
- 루프가 성립하기 위한 전제 3종을 `checkdocs.sh`가 검사한다 — 선행 ID 실재, **선행 순환 없음**
  (순환이 있으면 루프는 "착수 가능한 작업 없음"만 반복하며 교착된다), 완료로 표시된 작업의
  산출물 실재. 요약표 ↔ 실제 작업 수 대조도 함께

- **`make test-integration`이 DB를 직접 띄운다** (`scripts/testdb.sh`, `postgres:18-alpine`).
  `ONDOLITH_TEST_DSN`이 있으면 그쪽이 우선한다. 컨테이너는 다음 실행을 위해 남고
  `make test-db-down`으로 지운다 — **사람이 DB를 준비하는 단계가 루프 안에서 사라졌다**
  ([D82](docs/82-execution-loop.md) 2절)
- 통합 테스트는 `DROP SCHEMA public CASCADE`로 시작한다. 그래서 `testdb.sh`는 **자기가 띄운
  컨테이너의 DSN만** 넘기고, 포트를 다른 컨테이너가 쥐고 있으면 **중단한다** — 남의 데이터를
  지우느니 실행하지 않는다

### Changed
- **홈이 사이트 유형에 맞는 것을 보여준다** (P-201, FR-710) — `shop` 이면
  신상품, 두 유형 모두 최근 글. 그동안 홈은 사이트 이름 하나뿐이었다: Phase 1
  시절의 자리 채움이 Phase 2·3 이 끝난 뒤에도 남아 있었고, 스타일시트에는
  `.cards`·`.card-list`·`.hero-eyebrow` 규칙이 **쓰이지 않은 채** 있었다.
  데이터가 없으면 그 절을 그리지 않는다 — 빈 상자는 고장으로 보인다
- **로그인·회원가입·내 계정 화면이 한 축에 선다** — 제목은 화면 폭, 폼은
  24rem, 소셜 버튼은 또 화면 폭이라 어느 것이 본체인지 읽히지 않았다.
  로그인 화면에는 소셜 로그인 블록이 **통째로 두 번** 있어 버튼이 6개로
  그려지고 있었다
- **페이지 「만들기」와 「고치기」가 서로 다른 권한이 됐다** (`page.create` /
  `page.update`) — 둘 다 `page.update` 로 받고 있어서 `page.create` 는 역할
  편집기에 나타나기만 하고 아무 데서도 판정되지 않았다. 「고칠 수만 있고 만들
  수는 없는」 역할을 만들 수 없었다
- **주문 취소는 `order.cancel` 을 요구한다** — A-506 의 상태 전이가
  `order.update` 만으로 「취소」까지 보낼 수 있었다. 기본 역할(operator)은 두
  권한을 함께 갖고 있어 동작은 그대로다
- **관리자 화면의 클래스 13개와 `fieldset`·`legend`·`label`·`dl` 에 규칙이
  없었다** — 환불 화면의 「환불할 품목」 상자가 브라우저 기본 테두리였고,
  체크박스와 수량칸이 세로로 갈라졌으며, 제출 버튼은 작은 기본 버튼이었다.
  `<label>` 이 88개 있는데 규칙은 `.adm-field label` 뿐이었다. 품목 한 건이
  한 줄이 되도록 잡고(어느 수량이 어느 품목인지 눈으로 잇지 않게), 프론트
  이름을 쓰던 클래스(`error`·`form`·`filters`)를 관리자 이름으로 맞췄다
- **관리자 화면 13개의 입력 요소 40개에 스타일이 없었다** — 규칙이
  `.adm-field`·`.adm-filterbar` 안에만 걸려 있었다. 옵션·재고 편집기, 반품
  처리, 주문 목록 등이 브라우저 기본 모양으로 그려졌다. 프론트와 같은 방식으로
  요소 선택자에 건다
- **폼 요소가 어디에 놓이든 같은 모양이 됐다** — 입력칸·선택상자·버튼 규칙이
  `.form`·`.board-filter`·`.sort` 세 곳에 복사돼 있었고, 그 밖의 것(장바구니
  수량칸과 「변경」 버튼 등)은 브라우저 기본 모양이었다. 요소 선택자 한 벌로
  합치고 컨테이너는 배치만 정한다. 체크박스·라디오·파일 입력은 따로 다룬다
- **좁은 화면 규칙이 무시되고 있었다** — `@media (max-width: 767px)` 가 파일
  중간에 있어서 뒤에 오는 규칙이 같은 특정도로 덮어썼다. 블록을 맨 끝으로
  옮겼고, 폼 폭 해제·필터줄 세로 배치·상품 카드 1열·긴 낱말 줄바꿈을 더했다
- **게시판·상품·주문·장바구니 화면에 스타일이 붙었다** — 내장 테마의 템플릿이
  쓰는 클래스 **45개에 CSS 규칙이 아예 없었다.** 마크업은 그려지고 브라우저는
  오류를 내지 않으므로, 게시판 목록의 표가 한 줄로 뭉쳐 있는 동안 통합 테스트
  900건이 전부 초록이었다. 표·배지·페이지네이션·합계·소셜 버튼·필터줄을
  1b 팔레트로 채웠고, 다시 벌어지면 `internal/theme` 의 테스트가 깨진다
- **디자인이 「1b 모던 소프트」로 바뀌었다** — 둥근 모서리(16px)·부드러운
  그림자·넉넉한 여백·따뜻한 중성 배경. 관리자·프론트·설치 화면이 같은 팔레트를
  쓴다. 이전 기본값은 3px 모서리에 그림자가 없는 「1a 클래식」이었다.
  `admin.theme` 설정으로 이전 안(`1a`)을 포함해 다섯 안 중 하나를 고를 수 있다
- **새 토큰을 발급하면 같은 사용자의 미사용 토큰이 무효화된다** — 비밀번호
  재설정과 이메일 인증 양쪽. 유효한 링크가 여러 개 살아 있으면 메일함 하나가
  새는 것으로 오래된 링크까지 전부 쓸 수 있다

### Fixed
- **selftest 의 docker 부재 검사 두 건이 macOS 배치를 가정하고 있었다.** "docker
  가 없으면 건너뛰지 않고 거부한다" 를 확인하려면 docker 가 안 보이는 PATH 가
  필요한데, 두 판 모두 환경에 기댔다: `PATH=/usr/bin:/bin` 은 docker 가
  `/usr/local/bin` 인 macOS 에서만 통했고(Ubuntu 는 `/usr/bin/docker`),
  앞자리에 실행 불가 shim 을 두는 판은 **`command -v` 가 실행 불가 항목을
  건너뛰고 PATH 를 계속 훑기** 때문에 애초에 통하지 않았다. 그래서 CI 는 매
  푸시마다 빨간불이었다. 이제 가리는 대신 **없앤다** — 실행 가능한 것만
  심링크로 모으고 docker 만 뺀 디렉터리를 만들고, **그 디렉터리가 의도대로인지를
  먼저 단언한다**(docker 는 안 보이고 sh 는 보인다). 그 단언이 없으면 앞선 두
  판처럼 검사가 엉뚱한 이유로 통과·실패한다
- **`make ui`가 영원히 멈출 수 있었다.** `CDP.send()`에 타임아웃이 없어, 브라우저가
  응답을 멈추는 순간(렌더러 정지·소켓 유실) 프라미스가 풀리지 않고 게이트가 그대로
  매달렸다 — 실측으로 감사 하나가 **CPU 0%로 한 시간**을 매달려 있었고 로그는
  마지막 줄에서 멈춘 채였다. **멈추는 게이트는 실패하는 게이트보다 나쁘다**:
  신호를 주지 않으면서 CI 잡의 시간을 통째로 쓴다. 연결이 끊기면 대기 중인 요청을
  전부 깨우고, 그것마저 없으면 60초에 죽는다. 「전부」에는 `once()`로 이벤트를
  기다리던 쪽도 든다 — 그쪽은 `waiting`이 아니라 `listeners`에 있어서 요청만
  깨우면 그대로 매달리고 죽은 클로저가 배열에 쌓인다. 끊긴 뒤의 호출은 60초를
  세지 않고 즉시 실패한다: 「무응답」은 브라우저가 느리다는 뜻으로 읽히는데
  실제로는 소켓이 죽은 것이고, 진단이 다르면 고치는 곳도 달라진다. 브라우저를 감사 도중에 죽여 확인했다
  — 고치기 전 무한 대기가 **2초**가 됐다.

  **1회성 확인은 저장소에 남지 않는다.** 타이머·이벤트 정리는 깨져도 화면에 표시가
  없고 감사가 조용히 매달리는 것으로만 드러나므로, `node --test`(내장, 의존성 없음)로
  회귀를 남기고 `make check`의 test 단계에 물렸다. 브라우저를 띄우지 않는다 —
  가짜 소켓이면 충분하고, 그래야 오프라인 게이트에서도 돈다. 네 가지 변이로
  확인했다: 타임아웃 제거·`clearTimeout` 제거·`close`/`error` 미처리·`error`만
  미처리. 그중 하나는 **테스트가 실패 대신 멈추는** 것을 드러내 테스트 자체에
  시간 제한을 걸게 했다 — 고치려는 병과 같은 모양이었다. `node --test`의 기본
  타임아웃은 **무한**이다: 이벤트 루프가 비면 취소해 주지만 타이머가 하나라도
  살아 있으면 그 방어가 돌지 않는다. 그래서 그물을 두 겹으로 둔다 —
  `--test-timeout`(바깥)과 테스트별 제한(안쪽). 타이머 누수 변이가 전에는
  죽여야 끝났는데 이제 20초에 `exit 1`로 스스로 끝난다.
- **게이트가 한글 대조를 19/24 건 건너뛰고 있었다.** macOS 의 BSD `sort` 는
  `en_US.UTF-8` 에서 한글을 같은 무게로 정렬해 **서로 다른 문자열을 하나로 접는다.**
  주문 상태 전이(FR-604) 24 건이 5 건으로 접힌 채 「5 건 일치」라고 초록이 났고,
  나머지 19 건은 D14 다이어그램과 `state.go` 사이에서 한 번도 대조되지 않았다.
  `checkdocs.sh` 는 94 곳에서 `sort -u`/`comm` 을 쓰므로 한글이 든 대조가 전부
  영향을 받았다. `LC_ALL=C` 로 바이트 정렬을 강제한다. 다행히 접혀 있던 19 건은
  실제로도 일치했다 — 이번에는 운이 좋았다는 뜻이지 검사가 돌았다는 뜻이 아니다
- **`mux.HandleFunc` 직접 등록을 막는 가드가 한 번도 실행된 적이 없었다.**
  테스트가 `grep` 을 띄우며 BRE `mux\.Handle\(` 를 넘겼는데 `\(` 는 여는 괄호라
  grep 이 "parentheses not balanced" 로 죽었고, 빈 출력을 「찾은 게 없다」로 읽어
  통과했다. 그동안 실제 등록 한 건(`handler_webhook.go` — 의도된 P-905 서브트리)이
  검토 없이 초록 아래에 있었다. 하위 프로세스 없이 `filepath.WalkDir` 로 읽고,
  예외를 이유와 함께 적었다.
  **예외는 파일이 아니라 줄이다.** 첫 판은 파일 단위로 열어 두었는데, 그러면
  허용된 파일 안에서는 몇 줄이든 통과한다 — `handler_webhook.go` 에
  `mux.HandleFunc("GET /admin/…")` 를 한 줄 더 넣어도 걸리지 않고, 그 라우트는
  화면 ID·클래스·권한 없이 서비스된다. 이 검사가 D15 4.4 의 유일한 백스톱인데
  백스톱 자신이 「이 파일은 통째로 믿는다」로 넓어져 있었다. 이제 `파일+등록 줄`
  다섯 개를 못 박고, 예외마다 **정확히 한 번** 나타나는지 대조한다: 0 회면
  등록이 사라졌거나 읽는 쪽이 고장 난 것이고, 2 회 이상이면 예외 하나로 여러
  라우트를 태운 것이다. 세 방향 모두 주입해 실패를 확인했다
- **A-508 결제 대사가 PG 를 끈 상태에서 패닉했다.** 가드가 `d.Gateway != nil` 로
  **클로저**를 봤는데 그 클로저는 절대 nil 이 아니었다 — nil 인 것은 반환값이고
  (`pgAdapterFor` 는 등록되지 않은 제공자에 `("", nil)`), `gw.Get` 이 nil 인터페이스에서
  죽었다. 저장소에 `recover` 가 없어 응답이 중간에 끊긴다. 가드를 호출자마다가
  아니라 모든 호출자가 지나는 `Store.Reconcile` 한 곳으로 옮기고, 조회하지 않은
  행을 「일치」가 아니라 「PG 가 설정되어 있지 않아 조회하지 않았다」로 표시한다
- **장바구니 수량에 오타를 내면 항목이 지워졌다.** `qty, _ = strconv.Atoi(...)` 가
  파싱 오류를 삼켜 `quantity=abc` 가 0 이 되고, 0 은 `UpdateCartItem` 의 삭제
  경로였다. 화면은 성공으로 보인다. D19 P-403 이 「0 이하·비정수는 400」이라고
  못 박은 그대로 400 을 돌려준다. 삭제는 `DELETE`(P-404)만 한다.
  `cart_quantity_integration_test.go` 가 **응답 코드가 아니라 표를 본다** — 400 을
  돌려주면서 행을 지워도 응답만 보는 단언은 통과하기 때문이다. 저장소에 PATCH 를
  걷는 테스트가 하나도 없어서(`grep MethodPatch` → 0건) 이 두 화면의 분기가
  통째로 검토 밖에 있었다
- **관리자 콘솔의 다크 모드가 죽어 있었다.** 서버가 기본값을 `data-admin-theme`
  로 찍어 채워서 admin.css 의 `:root:not([data-admin-theme])` 가 영원히 매치되지
  않았다 — 스무 줄짜리 다크 콘솔이 죽은 코드였고, `admin.theme` 을 저장하는
  화면도 없어 관리자 화면을 어둡게 만들 방법이 **아예 없었다.** 값 없는 `:root`
  가 기본안(1b)을 지게 하고 서버는 고르지 않았으면 아무것도 찍지 않는다 —
  그래야 「고르지 않았다」와 「1b 를 골랐다」가 구분된다. 라이트 배색은 그대로다.
- **장바구니에서 상품을 뺄 수 없었다.** `DELETE /cart/items/{id}`(P-404)는 라우트도
  핸들러도 있는데 **누를 자리가 없었다** — 수량을 0 으로 바꾸면 빠지지만 그것을
  아는 사람은 그 폼을 만든 사람뿐이다. 글 삭제(P-207)와 같은 결함이다.
- **결제 화면의 약관 상자가 브라우저 기본 모양**이었다. CSS 는 목록(`.terms > li`)
  용인데 마크업은 `<fieldset>` 이라 `label` 에는 아무것도 닿지 않았고, 얇은 테두리
  상자 하나가 다른 폼과 전혀 다른 모양으로 떠 있었다. 시행 중인 약관이 없으면
  상자도 오지 않는다 — 빈 「약관」은 무언가 못 불러온 것처럼 보이고, 결제 직전
  화면에서 그 인상은 비싸다.
- **머리·본문·바닥이 한 축에 서지 않았다.** `main` 만 가운데로 모으고 헤더·푸터는
  화면 전체 폭이라, 1280px 에서 사이트 이름은 x=34 · 본문 카드는 x=154 에서
  시작했다. 넘치지도 잘리지도 않아 `make ui` 가 재지 못했다 — 지금은 잰다.
- **값이 없는 커스텀 필드가 빈 회색 막대**로 본문 위에 떴다. 정의가 있으면 열던
  것을 값이 있을 때만 연다.
- **장바구니의 「주문하기」가 밑줄 링크**였다. 이 화면에서 다음으로 갈 유일한
  길인데 상품 화면의 「장바구니에 담기」·결제 화면의 「결제하기」와 모양이 달라,
  같은 흐름의 한 걸음인데 혼자 눈에 띄지 않았다.
- **접힌 관리자 사이드바에서 그룹 제목이 숨겨져** 링크 스물다섯 개가 한 덩어리로
  깔렸다. 그 상태에서 「주문」을 찾으려면 전부 읽어야 한다.
- **공개 화면이 관리자 세션으로만 재어지고 있었다.** 홈·상품·게시판·검색을
  방문자가 보는 상태로는 한 번도 재지 않았다 — 사이트에 처음 오는 사람이 밟는
  경로가 바로 거기다. 세 역할이 모두 본다 (감사 216 → 276 조합).
- **결제 화면의 약관이 늘 비어 있었다.** 시드가 시행일 30일 뒤인 약관만 만들어,
  약관이 붙은 상태로 그려진 적이 없다. 빈 표가 결함을 숨기는 것과 같은 모양이다.
- **좁은 화면(375px)에서 깨지던 자리 — 첫 감사에서 188건** (`make ui`)
  - **관리자 표 아홉이 가로 스크롤 상자 밖에 있었다.** `.adm-table` 은
    `min-width: 660px` 이라 그보다 좁은 화면에서 반드시 넘치는데, 감싸미가
    없으면 그 넘침이 **페이지 전체의 가로 스크롤**이 된다
  - **글쓰기 폼이 상자 밖으로 28px 밀려 있었다.** 그리드 아이템의 기본
    `min-width: auto` 때문에 열이 파일 선택 칸의 min-content(321px) 아래로
    내려가지 못했다 — 브라우저에서 열을 재 보고 알았다
  - **관리자 머리글이 화면 밖으로 14px 나갔다.** 사용자 이름이 줄지 않았다
  - **머리글의 링크가 22px** 이라 좁은 화면에서 옆 링크를 같이 누르게 됐다
  - **버튼 높이가 42·44·47·92px 네 종류였다.** 기본값이 둘이었고, 좁은
    머리글에서 「로그아웃」의 글자가 두 줄로 접혀 92px 이 됐다
  - **일곱 화면의 표가 빈 상태로만 그려지고 있었다** — 행이 없으면 열이 내용
    없이 좁아져 넘치지 않으므로, 그 화면의 반응형 동작은 검사되지 않은 채
    통과한다. 감사가 그것을 세어 알린다
- **`/admin/comments` 가 게시판을 고르기 전에도 「댓글이 없습니다」라고 했다** —
  형제 화면(A-307·A-309)은 「게시판 주소를 입력하세요」다. 운영자는 그 게시판에
  댓글이 없다고 믿게 된다
- **잘못된 입력이 서버 오류(500)가 되던 자리 열넷** — 형식이 깨진 값이 `uuid`
  컬럼과 비교되면 PostgreSQL 이 22P02 로 터지고, 그 오류는 어느 도메인 오류와도
  일치하지 않아 500 이 된다. 라우트 표 전체를 훑어 찾았다:
  - 경로 조각(`{id}`) **열둘** — 라우트를 다는 지점에서 형식을 보고 404 로
    끝낸다. 저장소에서 옮기려면 `pgx.ErrNoRows` 를 다루는 60곳과
    `RowsAffected` 를 보는 29곳을 전부 고쳐야 하고, 하나를 빠뜨리면 그 하나가
    남는다. 새로 만드는 화면도 자동으로 보호된다.
    **만들기 화면은 자기 주소를 갖는다** (`/admin/pages/new` 등) — 첫 판은
    `{id}` 를 함께 쓰고 관문이 `new` 를 모든 `{id}` 라우트에서 통과시켰는데,
    그 값을 만들기로 다루는 핸들러는 넷뿐이라 나머지는 그대로 22P02 로 터졌다.
    `DELETE /cart/items/new` 는 **로그인 없이도** 500 을 낼 수 있었다
  - 폼의 uuid 필드 **여섯** — 장바구니의 `variant_id`(공개 라우트), 관리자의
    `post_id`·`comment_id`·`attachment_id`·카테고리 `parent_id`
  - **없는 것을 지우거나 고치면 500** 이던 곳 넷 — 페이지 저장·발행·삭제와
    메뉴 삭제. `d.fail` 이 그 판정(404)을 이미 갖고 있는데 쓰지 않았다
  - **설정 화면의 오류 경로가 화면을 못 그렸다** — 정상 경로는 `Settings` 를
    넘기고 오류 경로는 `Error` 만 넘겨, 검증에 걸릴 때마다 422 대신 500 이
    나갔다. 두 경로를 한 함수로 합쳤다

  훑는 검사 자체의 빈틈도 함께 막았다. 목록을 손으로 적으면 빠진다:
  경로 조각 이름은 **닫힌 집합**이고(새 `{postID}` 가 생기면 관문 밖에 남으므로
  선언을 강제한다), 폼의 `_id` 필드 목록은 **핸들러 소스와 대조**한다 —
  첫 판은 `attachment_id`·`board_id`·`new_variant_id` 를 빠뜨리고 아무도 읽지
  않는 이름 둘을 적어 두었다
- **장바구니의 상품 링크가 전부 404 였다** — 화면이 `/shop/p/{ProductID}`(UUID)
  를 그리는데 라우트는 `/shop/p/{slug}` 다. 두 검사 모두 초록이었다
- **커머스·주문 17개 화면에 컨테이너가 없었다** — 게시판 6개는 흰 패널
  (`.page`), 인증 11개는 좁은 단(`.auth`) 안에 있었는데, 상품 상세·장바구니·
  주문서·주문 상세는 배경 위에 글자만 떠 있었다. 브라우저에서 재 보니 상품
  상세의 `<h1>` 은 배경 투명·테두리 0·패딩 0 으로 972px 를 가로질렀다
- **글 삭제·댓글 수정·댓글 삭제·구매확정·인증 메일 재발송·교환 차액 결제·메뉴
  삭제·QR 라벨·관리자 환불과 반품 처리** — 전부 라우트도 핸들러도 화면도
  있는데 **누를 자리가 없었다**
- **`<p class="actions">` 안의 삭제 폼이 그 줄 밖으로 밀려났다** — `<p>` 의 내용
  모델은 phrasing content 라 파서가 `<form>` 을 만나면 문단을 먼저 닫는다.
  응답은 200 이고 렌더링도 성공하므로 브라우저에서 재 보고서야 알았다
- **A-401 사용자 목록의 「다음」에 조건이 없었다** — 사용자가 한 명인 사이트도
  무한히 다음 쪽을 제시했다. 링크를 따라가는 검사가 35쪽까지 걸어가 요청 제한에
  걸렸다
- **빈 `item_id` 가 500 이었다** — 반품·환불 폼에서 빈 값이 그대로 내려가
  `uuid` 컬럼과 비교하다 22P02 로 터졌다. 사용자 입력이므로 422 로 끝나야 한다
- `View.Can` 이 한 번도 채워지지 않았다 — 테마가 물어볼 수 있는 권한 목록
  (`theme.CanKeys`)을 닫힌 집합으로 두고 채운다
- **사용자 상세(A-402)와 출고 피킹(A-516)이 화면을 여는 것만으로 500 이었다** —
  전자는 `*time.Time` 이 nil 인데 값 메서드를 불렀고(이메일 미인증 계정이
  그 상태이며 대부분이 그렇다), 후자는 아직 아무것도 스캔하지 않은 상태에서
  nil map 을 색인했다. 둘 다 그 화면의 **정상 출발 상태**였다
- **D11 이 적은 라우트 19개가 구현과 달랐다** — `/admin/stock/*` 대
  `/admin/scan/*`, `/admin/payments/reconcile` 대 `/admin/reconcile`,
  A-513 의 `{id}` 유무, 그리고 브라우저 폼이 보낼 수 없는 `DELETE` 표기 8건.
  부팅 자체 점검은 **화면 ID 만** 보고 경로·메서드는 대조하지 않았다
- 표의 마지막 행 아래 테두리가 감싼 상자의 경계와 겹쳐 굵어 보였다
- **주문 상태 드롭다운이 그 화면이 일으킬 수 없는 전이를 제시했다** (A-506) —
  `Next` 가 전이표의 행위자 목록을 버리고 쌍만 냈다. 결제대기 주문의 유일한
  선택지가 「결제실패」였는데 그것은 결제 흐름만 일으킬 수 있어서, 고를 때마다
  422 가 났다. 고를 수 있는 것이 전부 실패하는 화면은 아무것도 못 하는 화면이다
- **관리자 화면이 금액과 시각을 원형으로 찍었다** — `378000`,
  `2026-08-07 22:48:49.259683 +0900 KST`. 관리자 렌더러에 표시 함수가 없었다.
  테마의 `money`·`date` 를 그대로 쓴다 — 앞뒤 화면에서 같은 주문의 금액이 다르게
  보이는 것이 환불액을 잘못 넣는 경로다. 구매자 화면 6곳의 시각도 같았다
- **🚨 상품에 옵션·조합을 만들 방법이 없어 아무것도 팔 수 없었다** (A-503) —
  화면은 이미 있는 조합의 SKU·가격·재고만 고칠 수 있었고, 조합을 만드는
  입력이 없었다. 새 상품은 조합이 0개라 장바구니에 담을 것이 없다. 스토어에
  `AddVariant` 는 있었지만 어떤 화면도 부르지 않았다. 옵션 그룹·값을 받아
  **곱으로 조합을 만든다** — 이미 있는 조합은 재고·SKU 를 지키려 건드리지 않는다
- **🚨 커머스 관리 화면 7개가 통째로 빈 화면이었다** — 상품 목록·주문 목록·주문
  상세·취소환불·반품교환·배송·카테고리. 템플릿이 `{{with .Data}}` 로 감쌌는데
  관리자 렌더러는 `.Data` 를 만들지 않고 값을 최상위로 병합한다. `with` 는
  nil 이면 블록을 건너뛰므로 제목만 남았다. 응답은 200 이라 화면 렌더 테스트가
  초록이었다
- 관리자 사이드바에 「약관」·「사업자 정보」가 두 번씩 나왔다
- 커머스 관리 화면의 표 5개에 스타일이 붙지 않았다 (`class="adm-table"` 누락)
- 장바구니·주문서의 합계에서 레이블과 금액이 두 줄로 갈라졌다
- **🚨 새로 설치한 사이트의 관리자가 관리자 화면에 들어갈 수 없었다** — 설치
  마법사는 `users.is_admin` 만 켰다. 권한은 역할이 정하고(D15), `is_admin` →
  `admin` 역할 백필은 **마이그레이션 시점에 한 번** 도는데 설치는 마이그레이션을
  먼저 돌리고 계정을 나중에 만든다. 그래서 백필은 늘 빈 테이블을 보고, 유일한
  관리자는 역할 없이 남아 `/admin` 에서 「권한이 없습니다」를 봤다 — 되돌릴
  화면이 관리자 화면 안에 있으므로 그 사이트는 손댈 방법이 없었다. 설치가
  계정과 역할을 한 문장에서 만든다
- **`/admin` 이 404 였다** — A-101 의 패턴이 `/admin/{$}` 라 정확히 `/admin/`
  만 받았고, ServeMux 의 슬래시 보정은 서브트리 패턴에만 붙는다. 주소창에
  치는 것은 거의 언제나 슬래시 없는 쪽이다. 308 로 보낸다
- **⚠️ 주문 이력이 있는 계정이 삭제되면 그 주문이 주인을 잃었다**
  (`00018_orders_user_restrict.sql`) — `orders.user_id` 외래키가 `SET NULL` 로
  만들어져 있었다. FR-212 는 그 계정을 지우지 못하게 막으라고 요구하고,
  문서(D30 3-1)와 코드 주석은 `RESTRICT` 라고 적고 있었는데 스키마만 달랐다.
  삭제는 거부되지 않고 주문의 주인만 조용히 사라졌다 — FR-212 가 막으려던
  상태를 정확히 만들어 낸 것이다. 제약을 `RESTRICT` 로 바꾼다.
  **되돌릴 수 있는 마이그레이션이다**(Down 이 `SET NULL` 로 되돌린다). 다만
  이미 주인을 잃은 주문은 복구할 방법이 없어 복구하지 않는다 — 그 주문은
  비회원 주문으로 남고 주문번호로 조회된다
- **가입·로그인 직후 로그인 화면으로 튕기는 경우가 있었다** — 세션이 언제 인증됐는지는
  애플리케이션 시계로, 그 세션을 무효화하는 기준선은 데이터베이스 시계로 적고 둘을
  비교했다. 데이터베이스가 몇 밀리초라도 앞서 있으면 방금 만든 계정의 세션이 자기
  기준선보다 과거로 보여 즉시 파기됐고, 다시 시도해도 시계 차이는 그대로라 반복됐다.
  이제 양쪽 다 데이터베이스 시계로 적는다 ([M16](.ai/MISTAKES.md))
- **이메일 인증 링크가 운영에서 전부 404였다** — 라우트는 `/verify/{token}` 인데 메일은
  `/verify?t=…` 를 보냈다. 통합 테스트가 자기 라우트 표를 따로 적어 두고 있어서 통과했다
  ([M14](.ai/MISTAKES.md)). 픽스처가 실제 라우트 표를 쓰고, 메일이 보낸 URL을 그대로
  따라가서 확인한다
- **테마 계약이 코어가 그리는 이름과 어긋나 있었다** — [D17](docs/17-theme-contract.md)은
  `auth/verify.html`·`board/form.html`·`comment/form.html` 을 약속했는데 코어는
  `verify-done.html`·`verify-failed.html`·`board/write.html`·`board/comment-edit.html` 을
  그렸다. 테마 작성자가 계약대로 만든 파일은 아무도 읽지 않았다. `sitemap.xml`·`robots.txt`
  는 애초에 코어 생성이라 갈아끼울 수 없다는 사실을 계약에 적었다. `checkdocs.sh`가
  코어가 그리는 이름이 계약에 있고 폴백 테마에 존재하는지 검사한다
- **이미 사용된 인증 링크 재방문이 400이었다** — 메일 클라이언트의 링크 프리페치가 토큰을
  먼저 태우기 때문에, 사람이 클릭했을 때 성공한 인증이 실패로 보였다. D19대로 조용히
  200을 돌려준다
- [D19](docs/19-screen-io.md)가 **자기 자신과 어긋나 있었다** — P-514·A-512를 추가하면서
  머리말 개수(61)와 대상 표를 안 고쳐 63개를 명세하면서 61개라고 적고 있었고, A-512 절은
  부록 뒤에 붙어 있었다. 대상 표·개수 진술이 절과 일치하는지 `checkdocs.sh`가 검사한다
- D19가 「미확정」으로 남긴 항목 중 **상위 문서가 이미 채운 12건**이 그대로 남아 있었다
  (길이 상한, `comments.parent_id`의 `ON DELETE`, 작업 로그 대상 8화면, 재인증 응답 코드,
  superuser 보유자 보호, 카테고리 계층 등). 결정문을 인용하도록 고치고 닫힌 경위를 남겼다
  ([M12](.ai/MISTAKES.md))
- [D80](docs/80-roadmap.md) 「기획 완결성」 표가 다섯 칸에서 낡아 있었다 — FR 87(실제 80),
  NFR 25(31), 화면 99(101), 커버리지 35종(36), 템플릿 42종(43). D19·D81·D50 행도 없었다.
  **표의 숫자를 각 문서에서 다시 세어 대조하는 검사**를 붙였다 ([M12](.ai/MISTAKES.md))
- 닫힌 미결을 가리키던 인용 2건(OPEN-15, OPEN-38). 표기가 `- **미결**:`이 아니면
  검사를 빠져나갔다 — 이제 **어느 문서의 어떤 형태든** `OPEN-##` 인용은 대장에 있어야 한다
- A-511 환불 확정 단계가 `order.refund`를 함께 요구한다는 [D13](docs/13-screens-admin.md)의
  결정이 [D15](docs/15-access-control.md) 2.2 `사용 화면` 열에 반영돼 있지 않았다
- 설치 마법사가 DB 접속 실패 사유를 화면에 그대로 보여주는데(FR-107), 그 에러에 DSN이
  섞이면 DB 비밀번호가 페이지에 뜬다. 지금은 pgx의 `redactPW`에만 걸려 있고 **확인하는
  테스트가 없었다** — pgx 자신도 소스 주석에서 "정적 문자열만 반환하는 편이 나을 것"이라고
  적는 성질이다. 접속 거부 경로의 회귀 테스트를 추가했다 (C5 / NFR-210)
- D81 작업 수 요약표가 실제와 어긋나 있었다 (Phase 3: 34 vs 40, 합계: 117 vs 123) —
  남은 분량을 잘못 알게 하는 값이다. 실제 행 수와 대조하는 검사를 붙였다
- 완료 기준이 비어 있던 요구사항 4건(FR-612, FR-704, FR-706, NFR-204)을 채우고,
  빈 완료 기준을 `checkdocs.sh`가 잡게 했다 ([M7](.ai/MISTAKES.md))
- `internal/app`·`internal/migrations`에 테스트가 없어 NFR-204(세션 쿠키 하드닝)와
  NFR-303(모든 마이그레이션에 `Down`)을 확인하는 것이 아무것도 없었다.
  쿠키 구성과 미들웨어 체인을 DB 없이 테스트 가능한 순수 함수로 분리해 검증을 붙였다
- `make test-integration`이 패키지를 지목하지 않고 `./...` 전체를 돌린다. 패키지를
  나열하는 것은 `-run` 으로 테스트 이름을 고르는 것과 같은 함정이다 ([M6](.ai/MISTAKES.md))
- `stdlib.OpenDBFromPool` 로 만든 `*sql.DB`를 닫아도 공유 풀이 살아 있다는 성질을
  통합 테스트로 고정했다. 부팅 전체가 이 성질에 걸려 있는데 확인하는 것이 없었다
- 설치 완료와 종료가 겹칠 때의 데이터 레이스. 정리 함수가 라이브 핸들러 옆의 비보호
  변수에 있어, 설치 핸들러 고루틴의 쓰기와 종료 시 메인 고루틴의 읽기가 동기화되지
  않았다. 핸들러와 정리 함수를 한 값으로 묶어 원자 교체하고 `sync.Once`로 멱등하게
  만들었다 ([M5](.ai/MISTAKES.md), FR-106)
- 설치 모드 경고 로그가 `-addr` 값과 무관하게 `localhost`를 표기하던 문제. 바인드
  주소를 있는 그대로 기록한다
- 문서 ID(`D00`~`D90`)와 결정 ID(`D0`~`D4`)의 접두사 충돌 해소 — 결정 ID를 `DEC-`로 <!-- checkdocs:allow-legacy-id -->
  분리했다 ([M2](.ai/MISTAKES.md))
- 루트 `CLAUDE.md`와 `.ai/CLAUDE.md`에 중복되어 있던 규칙을 루트로 일원화.
  `.ai/CLAUDE.md`는 포인터만 남는다

### 마이그레이션

**모든 마이그레이션은 `Down` 을 갖는다** (NFR-303). 다만 `Down` 이 있다는 것과
**되돌릴 수 있다**는 것은 다르다 — 컬럼을 지우는 `Down` 은 스키마를 되돌리지만
그 안에 있던 값은 되살리지 못한다.

> **되돌릴 수 없는 변경의 다운그레이드 경로는 「백업 복원」 하나다** (NFR-308).
> `goose down` 이 아니다. 그래서 업그레이드 전 백업이 절차의 첫 단계다
> ([D70](docs/70-operations.md) 「업그레이드 절차」).

이 릴리즈까지 적용되는 마이그레이션 16개:

| 파일 | 내용 |
|---|---|
| `00001_init.sql` | `users` · `settings` · 세션 테이블. 설치 마법사가 이것부터 적용한다 |
| `00002_rbac.sql` | `roles` · `permissions` · `role_permissions` · `user_roles` · 게시판 스코프. `users.is_admin` 은 **남겨 둔다** — 지우면 이 릴리즈로 돌아갈 길이 없다 (NFR-308) |
| `00003_rbac_seed.sql` | 기본 역할·권한 시드. Phase 1 권한 집합 |
| `00004_content.sql` | `pages` · `menus` · `settings` 확장 |
| `00005_auth_tokens.sql` | 메일 인증·비밀번호 재설정 토큰. 해시만 저장한다 |
| `00007_board.sql` | `boards` · `posts` · `comments` · `attachments` · 커스텀 필드(JSONB) |
| `00008_post_search.sql` | 전문검색 생성 컬럼(`simple` config + 접두 질의)과 인덱스 |
| `00009_board_seed.sql` | Phase 2 권한 시드 |
| `00010_oplog.sql` | `operation_logs` + **append-only 트리거**. 수정·삭제가 DB 수준에서 거부된다 |
| `00011_product.sql` | `products` · `product_variants` · `categories`. 조합 유니크와 SKU 부분 유니크 |
| `00012_order.sql` | `orders` · `order_items` · `carts` · `terms` · `order_agreements`. 주문 시점 스냅샷 |
| `00013_payment.sql` | `payments` · `refunds` · `refund_items` · `webhook_events`. 환불 누적 CHECK 와 부분 유니크 |
| `00014_fulfillment.sql` | `returns` · `return_items` · `shipments`. 처리 중 반품의 부분 유니크 |
| `00015_commerce_seed.sql` | Phase 3 권한 시드 |
| `00016_discount.sql` | `orders.discount_amount` · `order_items.discount_amount`. 배분 스냅샷 |
| `00017_webhook_secret.sql` | `payments.secret`. 토스 웹훅 대조의 유일한 상대 |

**이 목록에 ⚠️ 파괴적 변경은 없다.** 전부 추가만 하며, 각 `Down` 은 자기가 만든
것을 지운다 — 앞선 릴리즈의 데이터를 건드리지 않는다. 첫 파괴적 마이그레이션은
`00006_drop_is_admin.sql`(W2-01)이고, 그때 이 절에 표시가 붙는다.

### Changed
- CSRF를 `gorilla/csrf`에서 표준 라이브러리 `net/http.CrossOriginProtection`으로 변경.
  `gorilla/csrf`는 최신 v1.7.3에도 수정 버전이 없는 CVE-2025-47909 대상이다.
  근거: [DEC-2.1](.ai/DECISIONS.md)
